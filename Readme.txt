This repo demonstrates the model application using Linear Regression on one of the datasets provided by Sklearn.

Two models are developed, one with 2 features and the other one with all the features to see which model performs best.